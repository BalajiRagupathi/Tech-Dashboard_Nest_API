export declare class Dashboard {
    id: string;
    name: string;
    total_users: number;
    usage_count: number;
    usage_points: number;
    popularity: number;
    constructor(id: string, name: string, total_users: number, usage_count: number, usage_points: number, popularity: number);
}
